import styled from 'styled-components';

const WIDTH = 60;
const HEIGHT = 60;

export const CellRoot = styled.td`
width: ${WIDTH}px;
height: ${HEIGHT}px;
border: 1px solid #999;
font-size: 10px;
color: #22292F;
background: #FFF;
`;